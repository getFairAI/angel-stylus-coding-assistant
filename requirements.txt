# --- Web & API ---
fastapi>=0.110.0
uvicorn>=0.27.0

# --- HTTP & scraping ---
requests>=2.31.0
beautifulsoup4>=4.12.3

# --- Browser scraping (Stylus course + Stylus Saturdays) ---
# Requires the chromium browser at runtime: `playwright install chromium`
# (done in the Dockerfile). Ingestion jobs that need it fail gracefully if absent.
playwright>=1.40.0

# --- Vector database ---
# Pin to 1.5.x: 0.x→1.x had breaking API changes (delete(where={}) now rejected,
# stricter UUID handling). Upgrading beyond 1.5 needs retesting.
chromadb>=1.5.1,<1.6.0

# --- Embeddings / LLM runtime ---
ollama>=0.1.8

# --- MCP server (Streamable HTTP) ---
# Exposes retrieval as standard MCP tools at /mcp for external agent hosts.
fastmcp>=3.4.0,<4.0.0

# --- Utilities ---
pydantic>=2.6.0
python-dateutil>=2.9.0

# --- Test ---
pytest>=8.3.0
pytest-cov>=7.0.0
